// Importing the necessary packages
import React from "react"; // Importing the main React library
import ReactDOM from "react-dom"; // Importing the React DOM library
import App from "./App"; // Importing the App component from the App.js file in the same directory
import "bootstrap/dist/css/bootstrap.css"; // Importing Bootstrap CSS

// Render the App component to the root element in the index.html file
ReactDOM.render(<App />, document.getElementById("root"));
